# E2E Integration + P1 Stabilization Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Date**: 2026-03-09
**Scope**: `data_flow/01` (E2E) → `fastapi_backend/03` (Sweep) → `data_flow/02` (TTS) → `data_flow/03` (LTM)
**Goal**: Verify the core delegation round-trip end-to-end, then stabilize P1 flows.

**Tech Stack**: Python/pytest (backend), Bash (scripts)

---

## Active Tasks (from INDEX.md)

| # | Task | Priority | Status |
|---|------|----------|--------|
| 1 | Verify existing E2E tests (data_flow/01 mock) | P0 | TODO |
| 2 | Create mock callback shell script | P0 | TODO |
| 3 | Background Sweep (fastapi_backend/03) | P1 | TODO |
| 4 | TTS Flow verification (data_flow/02) | P1 | VERIFY |
| 5 | LTM Consolidation (data_flow/03) | P1 | VERIFY |
| 6 | Update INDEX.md | — | last |

---

### Task 1: Verify Existing E2E Tests (data_flow/01 mock 검증)

**Background:** `test_delegation_e2e.py`와 `test_callback_api.py`가 위임 round-trip을 이미 커버.
신규 테스트 불필요 — 기존 테스트를 실행하여 FastAPI 측 흐름을 검증한다.

**Step 1: Run existing tests**

```bash
cd backend && uv run pytest tests/api/test_delegation_e2e.py tests/api/test_callback_api.py -v
```

Expected: 모든 테스트 PASS.

실패 시: 에러를 분석하고 수정 후 재실행. 수정 내용을 커밋한다.

**Step 2: Commit (수정이 발생한 경우만)**

```bash
git add backend/
git commit -m "fix(delegation): fix failing E2E tests"
```

---

### Task 2: Create Mock Callback Shell Script

**Files:**
- Create: `backend/scripts/mock_callback.sh`

**Purpose:** FastAPI 단독으로 callback 흐름을 수동 테스트할 수 있는 도구.
NanoClaw 없이도 개발/디버깅 가능하게 한다.

**Step 1: Create the script**

`backend/scripts/mock_callback.sh`:

```bash
#!/usr/bin/env bash
# mock_callback.sh — Manually simulate a NanoClaw callback to FastAPI for debugging.
#
# PREREQUISITE: You must have a real pending task in STM.
# How to get one:
#   1. Start FastAPI: cd backend && uv run uvicorn src.main:app
#   2. Connect via WebSocket and send a message that triggers DelegateTaskTool
#   3. Copy the task_id from the FastAPI logs: "Task record: task_id=..."
#   4. Then run this script with that task_id and session_id
#
# Usage:
#   ./scripts/mock_callback.sh <task_id> <session_id> [done|failed]
#
# Example:
#   ./scripts/mock_callback.sh abc-123 session-xyz done

set -euo pipefail

if [ $# -lt 2 ]; then
  echo "Usage: $0 <task_id> <session_id> [done|failed]"
  echo ""
  echo "PREREQUISITE: task_id must exist in session STM pending_tasks."
  echo "Get it from FastAPI logs after triggering DelegateTaskTool."
  exit 1
fi

TASK_ID="$1"
SESSION_ID="$2"
STATUS="${3:-done}"
BASE_URL="${BACKEND_URL:-http://localhost:8000}"

if [ "$STATUS" = "done" ]; then
  SUMMARY="NanoClaw mock 완료 — 작업 성공."
else
  SUMMARY="NanoClaw mock 실패 — 타임아웃 시뮬레이션."
fi

echo "Sending $STATUS callback to $BASE_URL/v1/callback/nanoclaw/$SESSION_ID"
echo "  task_id:  $TASK_ID"
echo "  summary:  $SUMMARY"
echo ""

curl -s -w "\nHTTP Status: %{http_code}\n" \
  -X POST "$BASE_URL/v1/callback/nanoclaw/$SESSION_ID" \
  -H "Content-Type: application/json" \
  -d "{
    \"task_id\": \"$TASK_ID\",
    \"status\": \"$STATUS\",
    \"summary\": \"$SUMMARY\"
  }"
```

**Step 2: Make executable**

```bash
chmod +x backend/scripts/mock_callback.sh
```

**Step 3: Commit**

```bash
git add backend/scripts/mock_callback.sh
git commit -m "scripts: add mock callback script for FastAPI delegation debugging"
```

---

### Task 3: Background Sweep (fastapi_backend/03)

**PRD**: [`fastapi_backend/task-03-background-sweep.md`](../prds/feature/fastapi_backend/task-03-background-sweep.md)

**Goal**: TTL 초과 task를 `failed` 처리하는 주기적 백그라운드 스캔. FastAPI 서버 lifespan 내에서 asyncio 태스크로 실행.

**Step 1: Write test first (TDD)**

`backend/tests/services/test_background_sweep.py` 작성:
- `pending` 상태의 expired task가 `failed`로 마킹된다
- TTL 미만 task는 건드리지 않는다
- sweep 주기는 설정값으로 제어된다

```bash
cd backend && uv run pytest tests/services/test_background_sweep.py -v
```

Expected: FAIL (아직 구현 없음).

**Step 2: Implement**

구현 위치: `backend/src/services/task_sweep_service/` (새 서비스 디렉토리)
- `sweep.py`: TTL 스캔 로직
- `__init__.py`: 서비스 등록

`main.py` lifespan에 sweep 태스크 등록:
```python
asyncio.create_task(start_sweep_service())
```

설정은 `backend/yaml_files/`에 추가 (sweep interval, TTL 값).

**Step 3: Run tests**

```bash
cd backend && uv run pytest tests/services/test_background_sweep.py -v
```

Expected: PASS.

**Step 4: Lint**

```bash
cd backend && sh scripts/lint.sh
```

**Step 5: Commit**

```bash
git add backend/
git commit -m "feat(sweep): add background sweep service for expired task cleanup"
```

---

### Task 4: TTS Flow Verification (data_flow/02)

**PRD**: [`data_flow/task-02-tts-flow.md`](../prds/feature/data_flow/task-02-tts-flow.md)

**Goal**: 기존 TTS 구현 검증. 스트리밍 → 문장 감지 → TTS → Unity push 흐름이 올바른지 확인.

**Step 1: Read existing TTS implementation**

TTS 서비스 코드를 읽고 현재 동작을 파악한다.

**Step 2: Run TTS-related tests**

```bash
cd backend && uv run pytest tests/ -k "tts" -v
```

Expected: PASS.

실패 또는 누락된 케이스가 있으면 보완한다.

**Step 3: Commit (수정/추가된 경우만)**

```bash
git commit -m "verify(tts): confirm TTS streaming pipeline correctness"
```

---

### Task 5: LTM Consolidation Verification (data_flow/03)

**PRD**: [`data_flow/task-03-ltm-consolidation.md`](../prds/feature/data_flow/task-03-ltm-consolidation.md)

**Goal**: turn counter가 user message only로 계산되는지 확인 + synthetic message가 LTM consolidation에 포함되는지 검증.

**Step 1: Read existing LTM implementation**

LTM consolidation 관련 코드를 읽고 현재 동작을 파악한다.

**Step 2: Run LTM-related tests**

```bash
cd backend && uv run pytest tests/ -k "ltm" -v
```

Expected: PASS.

실패 또는 누락된 케이스가 있으면 보완한다.

**Step 3: Commit (수정/추가된 경우만)**

```bash
git commit -m "verify(ltm): confirm turn counter and synthetic message consolidation"
```

---

### Task 6: Update INDEX.md

모든 태스크 완료 후 INDEX.md 상태를 업데이트한다.

| Task | 예상 변경 |
|------|-----------|
| data_flow/01 | `TODO` → `DONE` (mock 검증 완료 기준) |
| fastapi_backend/03 | `TODO` → `DONE` |
| data_flow/02 | `VERIFY` → `DONE` (문제 없을 경우) |
| data_flow/03 | `VERIFY` → `DONE` (문제 없을 경우) |

```bash
git add docs/prds/feature/INDEX.md
git commit -m "docs: update INDEX.md after E2E and P1 stabilization"
```

---

## Task Dependency Graph

```
Task 1 (E2E tests) ──┐
Task 2 (mock script) ─┤── Task 6 (INDEX update)
Task 3 (Sweep) ───────┤
Task 4 (TTS verify) ──┤
Task 5 (LTM verify) ──┘
```

Tasks 1–5는 모두 독립적. Task 6은 마지막.

## Build Order

1. **Task 1** (P0): 기존 테스트 Green 확인 — 가장 먼저
2. **Task 2** (P0): mock script — 빠른 작업, Task 1과 병렬 가능
3. **Task 3** (P1): Background Sweep — TDD로 구현
4. **Task 4 + 5** (P1): TTS/LTM 검증 — Task 3과 병렬 가능
5. **Task 6** (last): INDEX 업데이트
